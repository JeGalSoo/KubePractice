-- ============================================================
--  Flyway Migration V1: 초기 스키마 베이스라인
--
--  이 파일은 이미 존재하는 테이블들을 Flyway가 인식하게 하기 위한
--  baseline 파일입니다. 실제로는 backend-dev.yml의
--  `flyway.baseline-on-migrate: true` 설정으로 처리되지만,
--  버전 기록을 명확히 남기기 위해 baseline SQL을 작성해둡니다.
--
--  ⚠️ 이 파일은 실제로 실행되지 않습니다.
--     baseline-on-migrate: true 가 설정된 상태에서 Flyway는
--     기존 스키마를 V1 버전으로 간주하고, V2부터 순차 실행합니다.
-- ============================================================

-- (실행 없음 — baseline 마커 파일)
